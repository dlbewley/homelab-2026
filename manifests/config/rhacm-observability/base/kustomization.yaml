apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster-agnostic pieces only. Anything naming a StorageClass, an S3 endpoint
# or a disk size lives in overlays/<cluster>.
resources:
  - namespace.yaml
  - rbac.yaml
  - secretstore.yaml
