---
# The issuer everything else should reference. Signs leaf certificates using the
# root CA created above.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: homelab-ca
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  ca:
    secretName: homelab-ca-tls
