apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - externalsecretsconfig.yaml
  # Cluster-agnostic: every cluster has openshift-config/pull-secret.
  - clustersecretstore-pullsecret.yaml