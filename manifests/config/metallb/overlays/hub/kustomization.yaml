apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # 192.168.4.224-253, confirmed against the router's DHCP scope on 2026-08-14.
  # See the comment at the top of the file before changing the range.
  - addresspool.yaml
