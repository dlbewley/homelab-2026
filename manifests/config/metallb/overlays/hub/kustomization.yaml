apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # Uncomment after confirming the range does not collide with the DHCP scope
  # on 192.168.4.0/24. See the comment at the top of the file.
  # - addresspool.yaml
