apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # Names a specific vCenter, so it is cluster-specific.
  - externalsecret-vsphere.yaml
  - provider-vsphere.yaml
