apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # Search database persistence. In an overlay rather than base because it must
  # name a StorageClass, and that is cluster-specific.
  - search.yaml
  # ACM Credential for the lab vCenter. Topology in git, secrets from stores.
  - credential-vsphere.yaml
