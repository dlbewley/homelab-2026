apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # Uncomment once the NIC names are confirmed on every cnv node. Read the
  # warning at the top of the file first — a wrong NIC isolates the node.
  - nncp-vmdata.yaml
