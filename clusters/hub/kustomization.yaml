apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied once by hand (`oc apply -k clusters/hub`), then self-managed by the
# hub-root Application in root.yaml.
namespace: openshift-gitops

resources:
  - appproject.yaml
  - root.yaml
  - operators.yaml
  - config.yaml
